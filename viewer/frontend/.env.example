# API base URL for the viewer backend (no trailing slash).
# Omit or leave unset to use same-origin /api (e.g. when frontend and API are served together).
# For local dev with a separate backend: http://127.0.0.1:8000/api
# VITE_API_BASE=http://127.0.0.1:8000/api
